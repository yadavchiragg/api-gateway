# ─────────────────────────────────────────────────────────
#  Distributed API Gateway — Python Dependencies
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────

# Web framework — async-first, auto-generates OpenAPI docs
fastapi==0.115.5

# ASGI server — production-grade, used by Render
uvicorn[standard]==0.32.1

# Async Redis client — non-blocking I/O for rate limiting
redis[asyncio]==5.2.0

# Environment variable loader — reads from .env file
python-dotenv==1.0.1

# HTTP client for FastAPI testing utilities
httpx==0.27.2
